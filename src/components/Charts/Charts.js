import React from "react";
import MenuItem from "../MenuItem/MenuItem";
import "./index.scss";

const Charts = (p) => {
  return (
    <div className="Charts">
      Här kan du använda <code>p.hubConnection</code>
    </div>
  );
};

export default Charts;
